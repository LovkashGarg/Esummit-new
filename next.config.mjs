/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['images.unsplash.com', 'res.cloudinary.com','assets.aceternity.com', 'lh3.googleusercontent.com']
    }
};

export default nextConfig;
